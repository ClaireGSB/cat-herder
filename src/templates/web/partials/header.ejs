<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= typeof title !== 'undefined' ? title + ' - ' : '' %>cat-herder Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.2/font/bootstrap-icons.css" rel="stylesheet">
    <style>
        .status-pending { background-color: #6c757d; }
        .status-running { background-color: #0d6efd; }
        .status-done { background-color: #198754; }
        .status-started { background-color: #10cef8; }
        .status-failed { background-color: #dc3545; }
        .status-paused { background-color: #fd7e14; }
        .status-interrupted { background-color: #fd7e14; }
        .status-waiting_for_input { background-color: #ffc107; }

        .list-group-item.active {
      background-color: #0d6efd;
      color: #fff;
      border-color: #0d6efd;
        }
        .list-group-item.active .badge {
            color: #0d6efd;
            background-color: #fff;
        }
        
        .pipeline-step {
            border: 1px solid #dee2e6;
            border-radius: 0.375rem;
            margin-bottom: 0.5rem;
        }
        
        .pipeline-step.completed {
            border-color: #198754;
            background-color: #f8fff9;
        }
        
        .pipeline-step.running {
            border-color: #0d6efd;
            background-color: #f8f9ff;
        }
        
        .pipeline-step.failed {
            border-color: #dc3545;
            background-color: #fff8f8;
        }
        
        .log-viewer {
            background-color: #212529; /* Dark background for terminal look */
            color: #f8f9fa; /* Light text */
            border: 1px solid #495057;
            border-radius: 0.375rem;
            height: 70vh; /* Taller log view */
            overflow-y: auto;
            font-family: 'Courier New', monospace;
            font-size: 0.875rem;
            line-height: 1.4;
        }
        
        .log-content {
            white-space: pre-wrap;
            padding: 1rem;
            margin: 0;
        }
        
        .loading-spinner {
            display: none;
        }
        
        .task-stats {
            background-color: #f8f9fa;
            border-radius: 0.375rem;
            padding: 1rem;
        }
        
        .navbar-brand {
            font-weight: 600;
        }
        
        .table th {
            border-top: none;
        }
        
        .empty-state {
            text-align: center;
            padding: 4rem 2rem;
            color: #6c757d;
        }
        
        .token-usage {
            font-size: 0.875rem;
        }
        
        .token-usage .badge {
            margin-right: 0.5rem;
            margin-bottom: 0.25rem;
        }
        
        @keyframes spin {
            from { transform: rotate(0deg); }
            to { transform: rotate(360deg); }
        }

        .log-line .log-meta { color: #888; } /* Grey for timestamps */
        .log-line .log-assistant { color: #60a5fa; font-weight: bold; } /* Blue for ASSISTANT */
        .log-line .log-user { color: #4ade80; font-weight: bold; } /* Green for USER */
        .log-line .log-system { color: #c084fc; font-weight: bold; } /* Purple for SYSTEM */
        .log-line .log-info { color: #93c5fd; } /* Light blue for info like [TEXT] */
        .log-line .log-tool { color: #facc15; } /* Yellow for tool use */
        .log-line .log-quote { color: #f97316; } /* Orange for quotes */
        .log-line .log-error { color: #ef4444; font-weight: bold; } /* Red for errors/stderr */
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
            <a class="navbar-brand" href="/live">
                <i class="bi bi-gear-fill me-2"></i>
                cat-herder
            </a>
            <div class="navbar-nav ms-auto">
                <a class="nav-link" href="/live" title="Live Activity">
                    <i class="bi bi-broadcast-pin"></i>
                    Live Activity
                </a>
                <a class="nav-link" href="/history" title="Run History">
                    <i class="bi bi-clock-history"></i>
                    Run History
                </a>
            </div>
        </div>
    </nav>
    
    <div class="container mt-4">
